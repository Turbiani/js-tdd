const calc = {
  sum(a, b) {
    return a + b;
  },
  sub(a, b) {
    return a - b;
  },
  mult(a, b) {
    return a * b;
  },
  div(a, b) {
    return b === 0 ? 'Não é possível dividir por zero' : a / b;
  },
  div2(a, b) {
    return b === 0 ? 'Não é possível dividir por zero' : a / b;
  },
  div3(a, b) {
    return b === 0 ? 'Não é possível dividir por zero' : a / b;
  },
  div4(a, b) {
    return b === 0 ? 'Não é possível dividir por zero' : a / b;
  },
};

export default calc;
